# Database (외부 Docker 컨테이너: postgres-local)
# docker ps 확인: postgres-local (0.0.0.0:5432->5432/tcp)
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=techinsights_db
DB_USER=local-user
DB_PASSWORD=local_password

# Batch Job
JOB_NAME=crawlPostJob

# Monitoring (외부 컨테이너 사용)
# Prometheus: http://localhost:9090 (shared-prometheus)
# Grafana: http://localhost:3000 (shared-grafana)
