spring:
  config:
    import:
      - aws-parameterstore:/config/techinsights/dev/
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: never

gemini:
  api-key: ${GOOGLE_API_KEY}
  model: gemini-2.5-flash
  max-output-tokens: 4096